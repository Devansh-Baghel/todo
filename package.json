{
  "name": "todo-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "git subtree push --prefix dist origin production"
  },
  "devDependencies": {
    "vite": "^4.3.9"
  },
  "dependencies": {
    "dracula-ui": "^1.0.6",
    "sass": "^1.63.4"
  }
}
